info:
  name: Update Category
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8080/api/categories/1
  body:
    type: json
    data: |-
      {
        "name": "Fitness",
        "color": "#22c55e"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
