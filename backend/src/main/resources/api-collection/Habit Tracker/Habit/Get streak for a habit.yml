info:
  name: Get streak for a habit
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/api/habits/1/streak?weekStart=0
  params:
    - name: weekStart
      value: "0"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
