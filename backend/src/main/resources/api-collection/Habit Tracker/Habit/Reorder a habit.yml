info:
  name: Reorder a habit
  type: http
  seq: 3

http:
  method: PATCH
  url: http://localhost:8080/api/habits/2/reorder?direction=down
  params:
    - name: direction
      value: down
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
