info:
  name: Create One-off task for a day
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/one-offs
  body:
    type: json
    data: |-
      {
        "name": "Doctor appointment",
        "date": "2026-07-08"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
