info:
  name: Archive (Delete) a habit
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:8080/api/habits/3/archive
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
