info:
  name: Toggle completion for a one-off task
  type: http
  seq: 3

http:
  method: PATCH
  url: http://localhost:8080/api/one-offs/1/toggle
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
